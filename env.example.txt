# LLM API 配置
# 选择使用 DeepSeek 或 OpenAI
LLM_PROVIDER=deepseek

# DeepSeek API 配置（如果使用 DeepSeek）
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# OpenAI API 配置（如果使用 OpenAI）
OPENAI_API_KEY=your_openai_api_key_here

# 模型配置（可选，有默认值）
LLM_MODEL=deepseek-chat
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2000

# 数据库配置（可选）
DATABASE_URL=sqlite:///./data/conversations.db

# MCP 工具配置
# 天气查询服务（免费API）
# 1. 和风天气（推荐）：免费额度每天1000次，申请地址：https://dev.qweather.com/
WEATHER_SERVICE=qweather  # qweather, openweather, mock
QWEATHER_API_KEY=your_qweather_key_here

# 2. OpenWeatherMap：免费额度每天60次，申请地址：https://openweathermap.org/api
# WEATHER_SERVICE=openweather
# OPENWEATHER_API_KEY=your_openweather_key_here

# 3. 模拟数据（默认，无需配置）
# WEATHER_SERVICE=mock

# 高德地图 API（免费额度：每天6000次调用）
# 申请地址：https://console.amap.com/dev/key/app
# 免费额度足够个人开发和小型项目使用
AMAP_API_KEY=your_amap_api_key_here

# 12306 火车票查询 MCP 服务（免费，无需注册）
TRAIN_TICKET_SERVICE=mcp  # mock, mcp
TRAIN_TICKET_MCP_COMMAND=npx
TRAIN_TICKET_MCP_ARGS=-y,12306-mcp

# 时间查询 MCP 服务（免费，无需注册，使用 Python MCP 服务器）
TIME_MCP_COMMAND=python
TIME_MCP_ARGS=-m tools.time_mcp_server

# 记忆/知识库 MCP 服务（免费，无需注册）
# 适合：接待员（FAQ查询）、解决方案专家（解决方案库）
MEMORY_MCP_COMMAND=npx
MEMORY_MCP_ARGS=-y @modelcontextprotocol/server-memory

# 文件系统 MCP 服务（免费，无需注册）
# 适合：问题分析师（查看日志、配置文件）
# 注意：需要指定允许访问的目录（空格分隔）
FILESYSTEM_MCP_COMMAND=npx
FILESYSTEM_MCP_ARGS=-y @modelcontextprotocol/server-filesystem /app/data /app/logs /app/cache

# 日志配置（可选）
LOG_LEVEL=INFO
LOG_FILE=./logs/app.log
